Dùng tool.py để đổi ảnh hàng loạt , đổi bao nhieu ảnh cũng được không giới hạn 


ở dòng 354 của file js thực hiện sửa số lượng ảnh để hiển thị phù hợp ví dụ 34 ảnh thì     for (let i = 1; i <= 32; i++) {
nếu 12 ảnh thì     for (let i = 1; i <= 12; i++) {
Sửa thêm ở dòng 296     const maxImages = 32;  

Về nhạc thì thêm nhạc và đổi tên file và để trong thư mục style
quy định bắt buộc : style/nhac.mp3
